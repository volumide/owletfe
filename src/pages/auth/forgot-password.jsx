import Auth from "./common/common"

const ForgotPassword = () => {
  return <Auth header="Reset Password" caption="To reset your password, we will email you instructions"></Auth>
}

export default ForgotPassword
